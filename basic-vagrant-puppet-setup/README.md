# Basic Vagrant/Puppet Setup

Steps:
* Install appropriate versions of **Vagrant** and **VirtualBox**
* Run `vagrant init` in the dev. directory
* Update `Vagrantfile`
* Update `manifests/default.pp` for puppet settings
* Run `vagrant up`

> http://localhost:3000